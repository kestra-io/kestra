id: flow-trigger-preconditions-flow-a
namespace: io.kestra.tests.trigger.preconditions

labels:
  some: label

tasks:
  - id: only
    type: io.kestra.plugin.core.debug.Return
    format: "from parents: {{execution.id}}"