kestra:
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: local
    local:
      base-path: /tmp/unittest
  queues:
    min-poll-interval: 10ms
    max-poll-interval: 100ms
    poll-switch-interval: 5s
  server:
    liveness:
      enabled: false