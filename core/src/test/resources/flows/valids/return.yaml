id: return
namespace: org.kestra.tests

tasks:
- id: date
  type: org.kestra.core.tasks.debugs.Return
  format: "{{taskrun.startDate}}"
- id: task-id
  type: org.kestra.core.tasks.debugs.Return
  format: "{{task.id}}"
- id: flow-id
  type: org.kestra.core.tasks.debugs.Return
  format: "{{flow.id}}"