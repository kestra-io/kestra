id: flow-trigger-stable-condition-id-flow-a
namespace: io.kestra.tests.trigger.stable.condition.id

tasks:
  - id: only
    type: io.kestra.plugin.core.debug.Return
    format: "from flow-a: {{execution.id}}"
