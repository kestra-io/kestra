# Layered on top of the `test` environment to mirror a distributed worker: the full server
# configuration is present, but `kestra.server-type` is WORKER — what WorkerCommand.propertiesOverrides()
# sets — so no repository bean is registered.
kestra:
  server-type: WORKER
