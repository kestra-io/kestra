kestra:
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: local
    local:
      base-path: /tmp/unittest
  tasks:
    gcs:
      bucket: "kestra-unit-test"
    bigquery:
      project: "kestra"
      dataset: "kestra_unit_test"