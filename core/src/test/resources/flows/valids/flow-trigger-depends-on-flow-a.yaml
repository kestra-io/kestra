id: flow-trigger-depends-on-flow-a
namespace: io.kestra.tests.trigger.depends.on

labels:
  some: label

tasks:
  - id: only
    type: io.kestra.plugin.core.debug.Return
    format: "from parents: {{execution.id}}"
