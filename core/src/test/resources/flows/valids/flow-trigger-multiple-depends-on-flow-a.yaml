id: flow-trigger-multiple-depends-on-flow-a
namespace: io.kestra.tests.trigger.multiple.depends.on

labels:
  some: label

tasks:
  - id: only
    type: io.kestra.plugin.core.debug.Return
    format: "from parents: {{execution.id}}"
