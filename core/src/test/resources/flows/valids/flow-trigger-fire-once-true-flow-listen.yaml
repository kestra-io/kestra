id: flow-trigger-fire-once-true-flow-listen
namespace: io.kestra.tests.trigger.fire.once.true

triggers:
  - id: flow
    type: io.kestra.plugin.core.trigger.Flow
    dependsOn:
      - namespace: io.kestra.tests.trigger.fire.once.true
        flowId: flow-trigger-fire-once-true-flow-a
        states: [SUCCESS]
      - namespace: io.kestra.tests.trigger.fire.once.true
        flowId: flow-trigger-fire-once-true-flow-b
        states: [SUCCESS]
    window:
      fireOnce: true

tasks:
  - id: only
    type: io.kestra.plugin.core.debug.Return
    format: "It works"
