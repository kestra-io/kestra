id: return
namespace: org.floworc.tests

tasks:
- id: date
  type: org.floworc.core.tasks.debugs.Return
  format: "{{taskrun.startDate}}"
- id: task-id
  type: org.floworc.core.tasks.debugs.Return
  format: "{{task.id}}"
- id: flow-id
  type: org.floworc.core.tasks.debugs.Return
  format: "{{flow.id}}"