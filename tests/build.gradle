dependencies {
    annotationProcessor project(':processor')

    implementation project(':core')
    implementation project(':worker')
    implementation project(':scheduler')
    implementation project(':executor')
    implementation project(':indexer')
    implementation "io.micronaut:micronaut-runtime"
    implementation "io.micronaut.test:micronaut-test-junit5"

    implementation "org.junit.jupiter:junit-jupiter-engine"
    implementation "org.junit.jupiter:junit-jupiter-engine"

    api 'org.hamcrest:hamcrest:3.0'
    api 'org.hamcrest:hamcrest-library:3.0'
    api 'org.mockito:mockito-junit-jupiter'
    api 'org.assertj:assertj-core:3.27.7'

    // Declared here directly, not just via the root subprojects testImplementation block, so
    // BaseArchitectureTest compiles under any root build.gradle (for example the kestra-ee
    // combined build whose own subprojects config doesn't add archunit).
    testImplementation 'com.tngtech.archunit:archunit-junit5:1.4.1'
}
