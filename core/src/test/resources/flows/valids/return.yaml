id: return
namespace: io.kestra.tests

tasks:
- id: date
  type: io.kestra.core.tasks.debugs.Return
  format: "{{taskrun.startDate}}"
- id: task-id
  type: io.kestra.core.tasks.debugs.Return
  format: "{{task.id}}"
- id: flow-id
  type: io.kestra.core.tasks.debugs.Return
  format: "{{flow.id}}"