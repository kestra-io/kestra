---
title: {{ capitalizeFirst (replace title "-" " ") }}
editLink: false
---
# {{ capitalizeFirst (replace title "-" " ") }}

{{ description }}

{{ longDescription }}

{{~#each classPlugins }}
    {{~#if @key.title}}
## {{ @key.title }}
    {{else}}
## {{ @key.name }}
    {{/if}}
{{ @key.description }}

{{~#each this as | classes type | }}
### {{ capitalizeFirst type }}

{{#each classes as | class | ~}}
* [{{simpleName}}]({{ type }}/{{#and subgroup.name}}{{subgroup.name}}/{{/and}}{{ name }}.html)
{{/each ~}}

{{/each ~}}
{{/each ~}}