kestra:
  url: https://mysuperhost.com/subpath
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: local
    local:
      base-path: /tmp/unittest

  variables:
    globals:
      string: "string"
      int: 1
      bool: true

  tasks:
    defaults:
    - type: org.kestra.core.services.TaskDefaultServiceTest$DefaultTester
      values:
        doubleValue: 19
        arrays:
          - 2
        property:
          here: me
          lists:
          - val:
              key: test